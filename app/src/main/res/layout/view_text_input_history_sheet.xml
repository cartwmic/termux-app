<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <EditText
        android:id="@+id/text_input_history_search"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/text_input_history_search_hint"
        android:imeOptions="actionDone|flagNoFullscreen"
        android:importantForAutofill="no"
        android:inputType="text"
        android:maxLines="1" />

    <TextView
        android:id="@+id/text_input_history_placeholder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="24dp"
        android:textStyle="italic"
        android:visibility="gone" />

    <ListView
        android:id="@+id/text_input_history_list"
        android:layout_width="match_parent"
        android:layout_height="240dp"
        android:divider="@null"
        android:dividerHeight="0dp" />

    <TextView
        android:id="@+id/text_input_history_clear_all"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackground"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:text="@string/text_input_history_clear_all"
        android:textStyle="bold" />

</LinearLayout>
